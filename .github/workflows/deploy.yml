name: Deploy to Hugging Face Space
on:
  push:
    branches:
      - master # Triggers when you push to master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Push to Hugging Face
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git push https://Vishnuporkulath:$HF_TOKEN@huggingface.co/spaces/Vishnuporkulath/career-pilot master:main -f
